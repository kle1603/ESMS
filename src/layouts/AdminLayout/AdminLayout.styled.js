import styled from "styled-components";
import { Layout } from "antd";

const {Sider} = Layout;

export const StyleSider = styled(Sider) `

.ant-layout-sider-children {
        background-color: #9a93ef;
    }

.ant-menu {
    background-color: #9a93ef;
    margin-top: 20px;
}
`